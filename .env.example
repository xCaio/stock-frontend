# Desenvolvimento local (usa proxy do Vite — evita CORS)
VITE_API_URL=/api

# Produção (frontend hospedado separadamente — exige CORS no backend)
# VITE_API_URL=https://stock-production-d03d.up.railway.app
